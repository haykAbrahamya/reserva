// Formatters live in @reserva/shared. Re-exported here so existing
// `@/utils/format` imports keep working unchanged — including `fmtServicePrice`
// (single amount for fixed pricing, compact "min – max ֏" for ranges).
export * from '@reserva/shared'
