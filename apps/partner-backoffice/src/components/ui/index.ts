// Re-export the shared component library so existing `@/components/ui`
// imports keep working, plus app-specific domain wrappers.
export * from '@reserva/ui'
export { BookingBadge } from './BookingBadge'
