# API base URL the internal-backoffice talks to.
# Local dev defaults to http://localhost:4000/api/v1 (see src/services/http.ts).
# Production value lives in .env.production.
VITE_API_URL=http://localhost:4000/api/v1
