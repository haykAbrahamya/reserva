{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    // The base config asks for vite/client ambient types, which only the
    // apps depend on. A library package needs no ambient types at all.
    "types": [],
    "noUnusedLocals": false
  },
  "include": ["src"]
}
